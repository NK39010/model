这是一个非常好的问题，也是很多初学者容易忽略的一点。

**MAFFT 的工作到输出比对文件就结束了，它不会告诉你“生物学结论”。真正的价值来自于对 MAFFT 输出结果的下游分析。**

下面以 MAFFT 为中心，介绍一条完整的数据分析流程。

```text
原始序列（FASTA）
        │
        ▼
      MAFFT
        │
        ▼
 比对结果（Alignment）
        │
 ┌──────┼──────────┬──────────┬─────────┐
 ▼      ▼          ▼          ▼         ▼
保守性   系统发育树  共识序列   SNP分析   Gap分析
分析      构建
```

---

# 第一类：查看 Alignment Quality（最基础）

MAFFT 输出：

```fasta
>Human
ATGC-TAGCTA
>Mouse
ATGCATAGCTA
>Dog
ATGC-TAGCTA
```

首先要做的是检查：

### ① Alignment Length（比对长度）

例如：

```
原始长度：
Human 100 aa
Mouse 102 aa
Dog 99 aa

Alignment：
110 aa
```

说明：

MAFFT 插入了 Gap。

---

### ② Gap 数量

例如：

```
Human 3
Mouse 0
Dog 5
```

可以统计：

```
Gap Ratio

Human
3 /110=2.7%
```

Gap 太多：

可能：

* 序列质量不好
* 同源性太低
* 参数不合适

---

### ③ 每列 Gap

例如：

```
A A A
T T T
G G -
C C -
```

第三列：

```
Gap Frequency

33%
```

第四列：

```
66%
```

很多论文：

Gap >50%

直接删掉。

这就是：

trimAl

Gblocks

做的事情。

---

# 第二类：保守性分析（最常见）

例如：

```text
Human  A
Mouse  A
Dog    A
Fish   A
```

这一列：

100%

Conserved。

另一列：

```text
Human  G
Mouse  A
Dog    G
Fish   T
```

说明：

变异较多。

可以计算：

```
Conservation Score

0~1
```

例如：

```
Position 10

1.00
```

Position 20

```
0.25
```

然后画图：

```
Conservation

1.0 ────────■■■■■■■
0.8 ─────■■■■
0.6 ──■■
```

这种图：

论文特别常见。

---

# 第三类：Consensus Sequence（共识序列）

例如：

```
A
A
A
A
```

Consensus：

```
A
```

另一列：

```
A
A
G
A
```

Consensus：

```
A
```

最终得到：

```
Consensus DNA
```

例如：

```
ATGCTAGCTA...
```

很多：

Primer

Probe

都是这样设计。

---

# 第四类：Identity Matrix（序列相似性）

例如：

```
Human

Mouse

Dog
```

计算：

两两 Identity

得到：

|       | Human | Mouse | Dog |
| ----- | ----- | ----- | --- |
| Human | 100   | 98    | 96  |
| Mouse | 98    | 100   | 95  |
| Dog   | 96    | 95    | 100 |

可以画：

Heatmap。

这也是：

MEGA

经常做的。

---

# 第五类：系统发育树

MAFFT 输出：

```
aligned.fa
```

直接：

```
IQ-TREE
```

例如：

```
iqtree2 -s aligned.fa
```

输出：

```
tree.nwk
```

然后：

画树。

这是：

最经典流程。

---

# 第六类：SNP分析

例如：

```
Human

A
```

Mouse

```
G
```

Dog

```
A
```

这一列：

就是：

SNP。

统计：

```
共有多少 SNP

每个位点频率
```

---

# 第七类：Motif分析

例如：

Protein：

```
GGHGG

GGHGG

GGHGG
```

说明：

Motif：

```
GGHGG
```

高度保守。

很多：

活性位点：

就是这样找出来。

---

# 第八类：Entropy（信息熵）

很多病毒：

都会画：

```
Entropy

Position
```

例如：

```
1

0.01
```

Position 100

```
1.25
```

说明：

100 位：

变异特别多。

公式：

```
H=-Σpilog2(pi)
```

MAFFT：

不给。

但是：

Alignment

可以计算。

---

# 第九类：Sequence Logo

例如：

画成：

```
      A
      A
  G   A
ATGCTA...
```

Logo 越高：

越保守。

这就是：

WebLogo。

输入：

就是：

MAFFT 输出。

---

# 第十类：Bio-Learn 可以自动生成的分析报告

如果你准备把 MAFFT 集成到 Bio-Learn，我建议不要只是提供一个 `aligned.fa` 下载按钮，而是自动生成一个分析报告，让用户一眼就能看懂比对质量。

| 模块                   | 输入        | 输出                                |
| -------------------- | --------- | --------------------------------- |
| Alignment Statistics | MAFFT 输出  | 序列数、比对长度、Gap 数、Gap 比例             |
| Conservation         | Alignment | 每个位点保守性评分、保守区间                    |
| Consensus            | Alignment | 共识序列                              |
| Identity             | Alignment | 两两相似性矩阵、热图                        |
| Gap Analysis         | Alignment | Gap 分布、Gap 热图                     |
| Variable Sites       | Alignment | 可变位点、SNP 统计                       |
| Entropy              | Alignment | 每个位点信息熵曲线                         |
| Sequence Logo        | Alignment | Logo 图                            |
| Export               | Alignment | FASTA、Clustal、PHYLIP 等格式          |
| Downstream           | Alignment | 一键发送到 IQ-TREE、FastTree、trimAl 等工具 |

## 我建议的 Bio-Learn 工作流

对于你的软件，可以把 MAFFT 定位为**整个分析流程的第一步**，后续自动衔接分析，而不是让用户自己寻找其他工具：

```text
导入 FASTA
     │
     ▼
MAFFT 比对
     │
     ▼
┌───────────────────────────┐
│ Alignment Dashboard（结果概览） │
├───────────────────────────┤
│ ✓ 序列数量                 │
│ ✓ 比对长度                 │
│ ✓ Gap 比例                 │
│ ✓ 保守性曲线               │
│ ✓ Sequence Logo            │
│ ✓ Identity Matrix          │
│ ✓ Consensus Sequence       │
│ ✓ Variable Sites           │
└───────────────────────────┘
     │
     ▼
选择下游分析
├── 系统发育树（IQ-TREE）
├── 保守结构域分析
├── 引物设计（基于共识序列）
├── 变异/SNP 分析
└── 导出多种格式
```

这样的设计能让用户从一份 FASTA 文件开始，经过 MAFFT 后直接得到可解释的统计结果和后续分析入口，而不是仅仅得到一份难以阅读的比对文件。
